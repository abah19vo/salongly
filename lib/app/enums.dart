enum UserType {
  admin('admin'),
  client('client');

  final String value;
  const UserType(this.value);
}

enum HaircutType {
  child('child'),
  women('women'),
  man('man');

  final String value;
  const HaircutType(this.value);
}
